#!/bin/sh
# Forgejo s6 service

export HOME=/config
export FORGEJO_WORK_DIR=/config
export FORGEJO_CUSTOM=/config/custom

cd $HOME
# Fix for Git hooks
ln -sf /bin/sh /bin/bash

exec /usr/local/bin/s6-setuidgid bsd /usr/local/sbin/forgejo web --config /config/custom/conf/app.ini
